#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

#define mp make_pair
#define pb push_back
#define pop pop_back
#define all(a) a.begin(), a.end()
#define rep_l(i, a, b) for (ll i = a; i < b; i++)
#define rep_i(i, a, b) for (int i = a; i < b; i++)
#define fast std::ios_base::sync_with_stdio(false), cin.tie(0), cout.tie(0)
#define fileio                                                                 \
    freopen("input.txt", "r", stdin), freopen("output.txt", "w", stdout)
#define tc                                                                     \
    int t;                                                                     \
    cin >> t;                                                                  \
    while (t--)

int main() {

    fast;
    fileio;

    tc {}

    return 0;
}